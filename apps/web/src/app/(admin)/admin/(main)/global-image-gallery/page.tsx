export default function GlobalImageGalleryPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Global Image Gallery</h1>
      <p className="mt-1 text-sm text-muted-foreground">
        Not sure if this is needed, but this could be the replica of the Global Image Gallery.
      </p>
    </div>
  );
}
