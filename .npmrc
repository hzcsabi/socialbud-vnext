# Hoist dependencies to root node_modules so workspace packages (shared, services)
# can resolve their deps when building on Vercel. Fixes "Cannot find module 'zod'"
# and "node_modules missing" in pnpm workspace builds.
node-linker=hoisted
